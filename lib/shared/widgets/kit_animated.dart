// Re-export the canonical KitAnimated implementation to avoid duplicate definitions.
// This file exists to preserve the old import path (`lib/shared/widgets/kit_animated.dart`).
// The canonical implementation lives at `lib/widgets/kit_animated.dart`.

export 'package:lumi/widgets/kit_animated.dart';
